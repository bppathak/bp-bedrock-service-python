apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
  namespace: bp-bedrock
  labels:
    app: backend
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
        - name: backend
          # Replace with your registry image (built from backend_python/Dockerfile)
          image: python-backend:latest 
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
              name: http
          envFrom:
            - configMapRef:
                name: backend-config
            - secretRef:
                name: backend-secret
          env:
            - name: VITE_API_BASE_URL
              value: "/api"
          volumeMounts:
            - name: local-data
              mountPath: /tmp/bp-bedrock-service
      volumes:
        - name: local-data
          emptyDir: {}